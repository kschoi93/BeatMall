<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.beetmall.sshj.admin.dao.AdminBoardDAO">
 	<select id="noticeBoardList" resultType="com.beetmall.sshj.admin.vo.AdminBoardVO">
 		select * from(
	    	select * from(
 				select infonum, infotype, infotitle, to_char(infowritedate, 'YY/MM/DD') infowritedate from notice order by infonum desc
			)
			where <![CDATA[rownum<=]]>${pageNum}*${onePageRecord} order by infonum)
			where <![CDATA[rownum<=]]><if test="pageNum==totalPage">${lastPageRecord}</if>
        							  <if test="pageNum!=totalPage">${onePageRecord}</if>
        order by infonum
 	</select>
 	<select id="noticeBoardView" resultType="com.beetmall.sshj.admin.vo.AdminBoardVO">
 		select infonum, infotitle, infowritedate, infoattach, infocontent from notice where infonum=#{param1}
 	</select>
 	<insert id="boardInsert" parameterType="com.beetmall.sshj.admin.vo.AdminBoardVO">
 		insert into board(no, subject, content, ip, userid) values(boardsq.nextval, #{subject}, #{content}, #{ip}, #{userid})
 	</insert>
 	<update id="boardUpdate" parameterType="com.beetmall.sshj.admin.vo.AdminBoardVO">
 		update board set subject=#{subject}, content=#{content} where no=#{no} and userid=#{userid} 
 	</update>
 	<select id="reviewListA" resultType="com.beetmall.sshj.admin.vo.AdminBoardVO">
 		select infonum, infotype, infotitle, to_char(infowritedate, 'YY/MM/DD') infowritedate from notice order by infonum desc
 	</select>
</mapper>